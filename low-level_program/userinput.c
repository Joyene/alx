#include <stdio.h>

/**
  * main - Entry point
  *
  * Return: Always (success)
  */
int main(void)
{
	int myNum;

	printf("Type a number and press enter: \n");

	scanf("%d", &myNum);

	printf("The number is: %d\n", myNum);

	return (0);
}
