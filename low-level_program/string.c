#include <stdio.h>

/**
  * main - Entry point
  *
  * Return: Always (success)
  */
int main(void)
{
	char greetings[] = "Hello World";

	printf("%s\n", greetings);

	printf("%c\n", greetings[4]);

	return (0);
}
