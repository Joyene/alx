#include <stdio.h>

/**
  * main - Entry point
  *
  * Return: Always 0 (success)
  */
int main(void)
{
	int i, j;

	int a[2][3] = { {0,0}, {1,1}, {2,2} };

	for (i = 0; i < 2; i++)
	{
		for (j = 0; j < 5; j++)
		{
			printf("a[%d][%d] = %d\n", i,j, a[i][j] );
		}
	}
	return (0);
}
